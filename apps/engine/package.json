{
  "name": "@transforma/engine",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --outfile=dist/main.js --bundle --platform=node --target=node22  --format=esm --packages=external",
    "dev": "tsx watch --title=api --env-file=.env ./src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fastify": "^5.4.0",
    "openai": "^5.10.2",
    "pg": "^8.16.3",
    "pino-pretty": "^13.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/pg": "^8.15.4",
    "esbuild": "^0.25.9"
  }
}
