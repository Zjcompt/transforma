{
  "name": "@transforma/engine",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch --title=api ./src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fastify": "^5.4.0",
    "pg": "^8.16.3",
    "pino-pretty": "^13.0.0"
  }
}
